import { PrivatizeOptions, Privatizer } from './types';
import Decimal from 'decimal.js';
export declare type ProbabilityDistribution = (sensitivity: Decimal, epsilon: Decimal, delta?: Decimal) => Decimal;
export declare function privatizeFactory<DATASET, OPTIONS extends PrivatizeOptions<DATASET>>(probDistFunc: ProbabilityDistribution): Privatizer<DATASET, OPTIONS>;
